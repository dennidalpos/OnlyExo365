{
  "tasks": [
    {
      "id": "delivery-release-baseline-divergence",
      "type": "refactor",
      "status": "todo",
      "risk": "medium",
      "priority": "medium",
      "context": "build/release-baseline.ps1 is a tracked but undocumented release path that creates artifacts/release outputs and optional git tags, while CI and docs use scripts/pack.ps1 plus scripts/agents/publish.ps1. Decide whether to remove it or fold it into the supported delivery flow."
    },
    {
      "id": "large-orchestration-files",
      "type": "refactor",
      "status": "todo",
      "risk": "medium",
      "priority": "medium",
      "context": "Several current orchestration files are large and high-blast-radius, including WorkerClient.cs, WorkerService.cs, IpcServer.cs, and multiple feature ViewModels/PowerShell command modules. Tests pass, so split incrementally by existing facets/use cases instead of broad rewrites."
    },
    {
      "id": "timing-dependent-viewmodel-tests",
      "type": "test",
      "status": "todo",
      "risk": "low",
      "priority": "low",
      "context": "Multiple ViewModel tests wait with fixed Task.Delay intervals. They are currently passing, but replacing fixed sleeps with deterministic completion signals would reduce flakiness."
    }
  ]
}
