<UserControl x:Class="ExchangeAdmin.Presentation.Views.DeletedMailboxesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Deleted Mailbox" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding DeletedMailboxes.StatusText}" Margin="12,0,0,0" VerticalAlignment="Center"
                           Foreground="{StaticResource SecondaryTextBrush}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Carica elenco" Command="{Binding DeletedMailboxes.LoadAllCommand}"
                        Style="{StaticResource PrimaryButton}"/>
                <Button Content="Annulla" Command="{Binding DeletedMailboxes.CancelCommand}"
                        Style="{StaticResource DangerButton}"
                        Visibility="{Binding DeletedMailboxes.IsLoading, Converter={StaticResource BoolToVisibility}}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding DeletedMailboxes.UpnQuery, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,12,0">
                <TextBox.Style>
                    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                        <Setter Property="Tag" Value="Inserisci UPN da verificare..."/>
                    </Style>
                </TextBox.Style>
            </TextBox>

            <Button Grid.Column="1" Content="Verifica UPN" Command="{Binding DeletedMailboxes.CheckUpnCommand}"
                    Style="{StaticResource PrimaryButton}"/>
        </Grid>

        <Grid Grid.Row="2">
            <DataGrid ItemsSource="{Binding DeletedMailboxes.Mailboxes}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Background="{StaticResource SecondaryBackgroundBrush}"
                      BorderBrush="{StaticResource BorderBrush}"
                      RowBackground="{StaticResource SecondaryBackgroundBrush}"
                      AlternatingRowBackground="{StaticResource TertiaryBackgroundBrush}"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                      ScrollViewer.IsDeferredScrollingEnabled="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Friendly Name" Binding="{Binding DisplayName}" Width="*"/>
                    <DataGridTextColumn Header="UPN" Binding="{Binding UserPrincipalName, TargetNullValue=—, FallbackValue=—}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding PrimarySmtpAddress}" Width="*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding RecipientTypeDetails}" Width="120"/>
                    <DataGridTextColumn Header="Stato" Binding="{Binding DeletionType}" Width="120"/>
                </DataGrid.Columns>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource TertiaryBackgroundBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource TertiaryBackgroundBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
            </DataGrid>

            <Border Background="#80000000"
                    Visibility="{Binding DeletedMailboxes.IsLoading, Converter={StaticResource BoolToVisibility}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                    <TextBlock Text="Caricamento cassette postali eliminate..." HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="#80000000"
                    Visibility="{Binding DeletedMailboxes.HasError, Converter={StaticResource BoolToVisibility}}">
                <Border Background="#3D0000" BorderBrush="{StaticResource ErrorBrush}"
                        BorderThickness="1" CornerRadius="4" Padding="16"
                        HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="Errore" FontWeight="SemiBold" Foreground="{StaticResource ErrorBrush}"/>
                        <TextBlock Text="{Binding DeletedMailboxes.ErrorMessage}" TextWrapping="Wrap" MaxWidth="400" Margin="0,4,0,0"/>
                        <Button Content="Riprova" Command="{Binding DeletedMailboxes.LoadAllCommand}" Margin="0,12,0,0"
                                Style="{StaticResource PrimaryButton}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Border>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
            <Button Content="Carica altri" Command="{Binding DeletedMailboxes.LoadMoreCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Visibility="{Binding DeletedMailboxes.HasMore, Converter={StaticResource BoolToVisibility}}"/>
        </StackPanel>
    </Grid>
</UserControl>
