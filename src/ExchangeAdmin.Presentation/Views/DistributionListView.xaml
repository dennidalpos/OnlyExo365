<UserControl x:Class="ExchangeAdmin.Presentation.Views.DistributionListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- List Panel -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Distribution Lists" FontSize="20" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding DistributionLists.StatusText}" Margin="12,0,0,0" VerticalAlignment="Center"
                               Foreground="{StaticResource SecondaryTextBrush}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Refresh" Command="{Binding DistributionLists.RefreshCommand}"
                            Style="{StaticResource PrimaryButton}"/>
                </StackPanel>
            </Grid>

            <!-- Search and Filter -->
            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Text="{Binding DistributionLists.SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,12,0"/>

                <CheckBox Grid.Column="1" Content="Include Dynamic" IsChecked="{Binding DistributionLists.IncludeDynamic}"
                          VerticalAlignment="Center"/>
            </Grid>

            <!-- Group List -->
            <DataGrid Grid.Row="2" ItemsSource="{Binding DistributionLists.DistributionLists}"
                      SelectedItem="{Binding DistributionLists.SelectedItem}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Background="{StaticResource SecondaryBackgroundBrush}"
                      BorderBrush="{StaticResource BorderBrush}"
                      RowBackground="{StaticResource SecondaryBackgroundBrush}"
                      AlternatingRowBackground="{StaticResource TertiaryBackgroundBrush}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Display Name" Binding="{Binding DisplayName}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding PrimarySmtpAddress}" Width="*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding GroupType}" Width="80"/>
                    <DataGridTextColumn Header="Members" Binding="{Binding MemberCount}" Width="60"/>
                </DataGrid.Columns>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource TertiaryBackgroundBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource TertiaryBackgroundBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
            </DataGrid>

            <!-- Load More -->
            <Button Grid.Row="3" Content="Load More" Command="{Binding DistributionLists.LoadMoreCommand}"
                    HorizontalAlignment="Center" Margin="0,12,0,0"
                    Style="{StaticResource PrimaryButton}"
                    Visibility="{Binding DistributionLists.HasMore, Converter={StaticResource BoolToVisibility}}"/>
        </Grid>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1" Width="4" Background="{StaticResource BorderBrush}"
                      HorizontalAlignment="Center" VerticalAlignment="Stretch"
                      ResizeDirection="Columns"
                      ResizeBehavior="PreviousAndNext"/>

        <!-- Details Panel -->
        <Grid Grid.Column="2" Margin="16,0,0,0">
            <!-- No Selection -->
            <TextBlock Text="Select a group to view details"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="{StaticResource SecondaryTextBrush}"
                       Visibility="{Binding DistributionLists.HasDetails, Converter={StaticResource InverseBoolToVisibility}}"/>

            <!-- Details Content -->
            <Grid Visibility="{Binding DistributionLists.HasDetails, Converter={StaticResource BoolToVisibility}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="{Binding DistributionLists.SelectedDetails.DisplayName}" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding DistributionLists.SelectedDetails.PrimarySmtpAddress}"
                                   Foreground="{StaticResource SecondaryTextBrush}"/>
                    </StackPanel>

                    <Button Grid.Column="1" Content="Back" Command="{Binding DistributionLists.BackCommand}"/>
                </Grid>

                <!-- Group Info -->
                <GroupBox Grid.Row="1" Header="Group Information" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Type:" Margin="0,0,12,4"
                                   Foreground="{StaticResource SecondaryTextBrush}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding DistributionLists.SelectedDetails.GroupType}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Alias:" Margin="0,4,12,4"
                                   Foreground="{StaticResource SecondaryTextBrush}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding DistributionLists.SelectedDetails.Alias}" Margin="0,4,0,4"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Managed By:" Margin="0,4,12,0"
                                   Foreground="{StaticResource SecondaryTextBrush}"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DistributionLists.SelectedDetails.ManagedBy, Converter={StaticResource ListToString}}" Margin="0,4,0,0"/>
                    </Grid>
                </GroupBox>

                <!-- Members -->
                <GroupBox Grid.Row="2" Header="Members">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Dynamic Group Warning -->
                        <Border Grid.Row="0" Background="#3D2200" BorderBrush="{StaticResource WarningBrush}"
                                BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,0,0,8"
                                Visibility="{Binding DistributionLists.IsDynamicGroup, Converter={StaticResource BoolToVisibility}}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Dynamic group - members are calculated based on filter."
                                           Foreground="{StaticResource WarningBrush}"/>
                                <Button Content="Preview Members" Command="{Binding DistributionLists.PreviewDynamicMembersCommand}"
                                        Margin="12,0,0,0" Padding="8,2"/>
                            </StackPanel>
                        </Border>

                        <!-- Members List -->
                        <ListBox Grid.Row="1" ItemsSource="{Binding DistributionLists.Members}"
                                 Background="{StaticResource SecondaryBackgroundBrush}"
                                 BorderBrush="{StaticResource BorderBrush}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Name}"/>
                                            <TextBlock Text="{Binding PrimarySmtpAddress}" FontSize="11"
                                                       Foreground="{StaticResource SecondaryTextBrush}"/>
                                        </StackPanel>

                                        <Button Grid.Column="1" Content="Remove"
                                                Command="{Binding DataContext.DistributionLists.RemoveMemberCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource DangerButton}" Padding="6,2"
                                                Visibility="{Binding DataContext.DistributionLists.CanModifyMembers, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Add Member -->
                        <Grid Grid.Row="2" Margin="0,8,0,0"
                              Visibility="{Binding DistributionLists.CanModifyMembers, Converter={StaticResource BoolToVisibility}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0" Text="{Binding DistributionLists.NewMemberIdentity, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,8,0"/>
                            <Button Grid.Column="1" Content="Add Member" Command="{Binding DistributionLists.AddMemberCommand}"
                                    Style="{StaticResource PrimaryButton}"/>
                        </Grid>
                    </Grid>
                </GroupBox>

                <!-- Load More Members -->
                <Button Grid.Row="3" Content="Load More Members" Command="{Binding DistributionLists.LoadMoreMembersCommand}"
                        HorizontalAlignment="Center" Margin="0,8,0,0"
                        Visibility="{Binding DistributionLists.MembersHasMore, Converter={StaticResource BoolToVisibility}}"/>
            </Grid>

            <!-- Loading Overlay -->
            <Border Background="#80000000"
                    Visibility="{Binding DistributionLists.IsLoadingDetails, Converter={StaticResource BoolToVisibility}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="150" Height="4"/>
                    <TextBlock Text="Loading details..." HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
