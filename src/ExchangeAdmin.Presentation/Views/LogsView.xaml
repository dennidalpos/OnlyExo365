<UserControl x:Class="ExchangeAdmin.Presentation.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

                       
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Logs" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding TotalCount, StringFormat='({0} entries)'}" Margin="12,0,0,0"
                           VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Copy" Command="{Binding Logs.CopyLogsCommand}"/>
                <Button Content="Clear" Command="{Binding Logs.ClearLogsCommand}"
                        Style="{StaticResource DangerButton}"/>
            </StackPanel>
        </Grid>

                           
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

                           
            <TextBox Grid.Column="0" Text="{Binding Logs.SearchFilter, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,12,0"/>

                                 
            <ComboBox Grid.Column="1" Width="120" SelectedValue="{Binding Logs.FilterLevel, Mode=TwoWay}"
                      SelectedValuePath="Tag" Margin="0,0,12,0"
                      ToolTip="Filter logs by minimum level">
                <ComboBoxItem Content="All" Tag="{x:Static local:LogLevel.Verbose}" IsSelected="True"
                              xmlns:local="clr-namespace:ExchangeAdmin.Contracts.Messages;assembly=ExchangeAdmin.Contracts"/>
                <ComboBoxItem Content="Debug+" Tag="{x:Static local:LogLevel.Debug}"
                              xmlns:local="clr-namespace:ExchangeAdmin.Contracts.Messages;assembly=ExchangeAdmin.Contracts"/>
                <ComboBoxItem Content="Info+" Tag="{x:Static local:LogLevel.Information}"
                              xmlns:local="clr-namespace:ExchangeAdmin.Contracts.Messages;assembly=ExchangeAdmin.Contracts"/>
                <ComboBoxItem Content="Warning+" Tag="{x:Static local:LogLevel.Warning}"
                              xmlns:local="clr-namespace:ExchangeAdmin.Contracts.Messages;assembly=ExchangeAdmin.Contracts"/>
                <ComboBoxItem Content="Error" Tag="{x:Static local:LogLevel.Error}"
                              xmlns:local="clr-namespace:ExchangeAdmin.Contracts.Messages;assembly=ExchangeAdmin.Contracts"/>
            </ComboBox>

                                 
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Border Background="#1E4D2A" CornerRadius="3" Padding="6,2" Margin="2,0"
                        ToolTip="Info">
                    <TextBlock Text="{Binding Logs.InfoCount}" Foreground="{StaticResource SuccessBrush}" FontSize="11"/>
                </Border>
                <Border Background="#4D3D00" CornerRadius="3" Padding="6,2" Margin="2,0"
                        ToolTip="Warning">
                    <TextBlock Text="{Binding Logs.WarningCount}" Foreground="{StaticResource WarningBrush}" FontSize="11"/>
                </Border>
                <Border Background="#4D1A1A" CornerRadius="3" Padding="6,2" Margin="2,0"
                        ToolTip="Error">
                    <TextBlock Text="{Binding Logs.ErrorCount}" Foreground="{StaticResource ErrorBrush}" FontSize="11"/>
                </Border>
            </StackPanel>
        </Grid>

                         
        <ListBox Grid.Row="2" ItemsSource="{Binding Logs.FilteredLogs}"
                 FontFamily="Consolas" FontSize="11"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 Background="{StaticResource SecondaryBackgroundBrush}"
                 BorderBrush="{StaticResource BorderBrush}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="85"/>
                            <ColumnDefinition Width="18"/>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding FormattedTimestamp}"
                                   Foreground="{StaticResource SecondaryTextBrush}"/>
                        <TextBlock Grid.Column="1" Text="{Binding LevelIcon}" FontWeight="Bold">
                            <TextBlock.Foreground>
                                <SolidColorBrush Color="{Binding LevelColor}"/>
                            </TextBlock.Foreground>
                        </TextBlock>
                        <TextBlock Grid.Column="2" Text="{Binding Source}"
                                   Foreground="{StaticResource SecondaryTextBrush}"/>
                        <TextBlock Grid.Column="3" Text="{Binding Message}" TextWrapping="NoWrap"
                                   TextTrimming="CharacterEllipsis"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                    <Setter Property="Padding" Value="4,2"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Level}" Value="{x:Static local:LogLevel.Error}"
                                     xmlns:local="clr-namespace:ExchangeAdmin.Contracts.Messages;assembly=ExchangeAdmin.Contracts">
                            <Setter Property="Background" Value="#2D0000"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Level}" Value="{x:Static local:LogLevel.Warning}"
                                     xmlns:local="clr-namespace:ExchangeAdmin.Contracts.Messages;assembly=ExchangeAdmin.Contracts">
                            <Setter Property="Background" Value="#2D2600"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</UserControl>
