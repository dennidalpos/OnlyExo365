<UserControl x:Class="ExchangeAdmin.Presentation.Views.MailboxListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:ExchangeAdmin.Presentation.Views">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

                           
        <Grid Grid.Column="0" IsEnabled="{Binding Mailboxes.IsLoading, Converter={StaticResource InverseBool}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

                       
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Mailboxes" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Mailboxes.StatusText}" Margin="12,0,0,0" VerticalAlignment="Center"
                           Foreground="{StaticResource SecondaryTextBrush}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Refresh" Command="{Binding Mailboxes.RefreshCommand}"
                        Style="{StaticResource PrimaryButton}"/>
                <Button Content="Cancel" Command="{Binding Mailboxes.CancelCommand}"
                        Style="{StaticResource DangerButton}"
                        Visibility="{Binding Mailboxes.IsLoading, Converter={StaticResource BoolToVisibility}}"/>
            </StackPanel>
        </Grid>

                                  
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

                           
            <TextBox Grid.Column="0" Text="{Binding Mailboxes.SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,12,0">
                <TextBox.Style>
                    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                        <Setter Property="Tag" Value="Search mailboxes..."/>
                    </Style>
                </TextBox.Style>
            </TextBox>

                                
            <ComboBox Grid.Column="1" Width="150" SelectedValue="{Binding Mailboxes.RecipientTypeFilter}"
                      SelectedValuePath="Tag">
                <ComboBoxItem Content="User Mailboxes" Tag="UserMailbox"/>
                <ComboBoxItem Content="Shared Mailboxes" Tag="SharedMailbox"/>
                <ComboBoxItem Content="Room Mailboxes" Tag="RoomMailbox"/>
                <ComboBoxItem Content="Equipment" Tag="EquipmentMailbox"/>
                <ComboBoxItem Content="All Types" Tag=""/>
            </ComboBox>
        </Grid>

                             

        <GroupBox Grid.Row="2" Header="New Shared Mailbox" Margin="0,0,0,12">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Text="{Binding Mailboxes.NewMailboxDisplayName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Setter Property="Tag" Value="Display name (es. Helpdesk Milano)"/>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBox Grid.Column="1" Text="{Binding Mailboxes.NewMailboxAlias, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Setter Property="Tag" Value="Alias (es. helpdesk.milano)"/>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBox Grid.Column="2" Text="{Binding Mailboxes.NewMailboxPrimarySmtpAddress, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Setter Property="Tag" Value="Primary SMTP address (es. helpdesk.milano@contoso.com)"/>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <Button Grid.Column="3" Content="Create" Command="{Binding Mailboxes.CreateMailboxCommand}" Style="{StaticResource PrimaryButton}"/>
            </Grid>
        </GroupBox>

        <Grid Grid.Row="3">
                         
            <DataGrid ItemsSource="{Binding Mailboxes.Mailboxes}"
                      SelectedItem="{Binding Mailboxes.SelectedMailbox}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Background="{StaticResource SecondaryBackgroundBrush}"
                      BorderBrush="{StaticResource BorderBrush}"
                      RowBackground="{StaticResource SecondaryBackgroundBrush}"
                      AlternatingRowBackground="{StaticResource TertiaryBackgroundBrush}"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                      ScrollViewer.IsDeferredScrollingEnabled="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Friendly Name" Binding="{Binding DisplayName}" Width="*"/>
                    <DataGridTextColumn Header="UPN" Binding="{Binding UserPrincipalName, TargetNullValue=—, FallbackValue=—}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding PrimarySmtpAddress}" Width="*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding RecipientTypeDetails}" Width="120"/>
                    <DataGridTextColumn Header="Alias" Binding="{Binding Alias}" Width="100"/>
                </DataGrid.Columns>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource TertiaryBackgroundBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource TertiaryBackgroundBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
            </DataGrid>

                                    
            <Border Background="#80000000"
                    Visibility="{Binding Mailboxes.IsLoading, Converter={StaticResource BoolToVisibility}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                    <TextBlock Text="Loading mailboxes..." HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

                          
            <Border Background="#80000000"
                    Visibility="{Binding Mailboxes.HasError, Converter={StaticResource BoolToVisibility}}">
                <Border Background="#3D0000" BorderBrush="{StaticResource ErrorBrush}"
                        BorderThickness="1" CornerRadius="4" Padding="16"
                        HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="Error" FontWeight="SemiBold" Foreground="{StaticResource ErrorBrush}"/>
                        <TextBlock Text="{Binding Mailboxes.ErrorMessage}" TextWrapping="Wrap" MaxWidth="400" Margin="0,4,0,0"/>
                        <Button Content="Retry" Command="{Binding Mailboxes.RefreshCommand}" Margin="0,12,0,0"
                                Style="{StaticResource PrimaryButton}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Border>
        </Grid>

                              
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                <Button Content="Load More" Command="{Binding Mailboxes.LoadMoreCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Visibility="{Binding Mailboxes.HasMore, Converter={StaticResource BoolToVisibility}}"/>
            </StackPanel>
        </Grid>

                         
        <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                      Background="{StaticResource BorderBrush}"
                      ResizeDirection="Columns"
                      ResizeBehavior="PreviousAndNext"/>

                              
        <views:MailboxDetailsView Grid.Column="2" DataContext="{Binding MailboxDetails}"
                                   Visibility="{Binding HasDetails, FallbackValue=Collapsed, Converter={StaticResource BoolToVisibility}}"/>
    </Grid>
</UserControl>
